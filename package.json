{
  "name": "insert-icons_slide-addon",
  "version": "1.1.2",
  "description": "Insert icons by ScriptAddict, Add-on for Google Slide",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/JeanRemiDelteil/insert-icons.git"
  },
  "scripts": {
    "buildPushDEV": "grunt --target=dev_SLIDE build_push && grunt --target=dev_SHEET build_push && grunt --target=dev_DOC build_push",
    "buildPushPROD": "grunt --target=prod_SLIDE build_push && grunt --target=prod_SHEET build_push && grunt --target=prod_DOC build_push"
  },
  "devDependencies": {
    "gas-lib": "latest",
    "grunt": "^1.0.1",
    "grunt-contrib-clean": "^1.0.0",
    "grunt-contrib-copy": "^1.0.0",
    "grunt-preprocess": "^5.1.0"
  }
}
